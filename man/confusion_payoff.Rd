% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confusion_payoff.R
\name{confusion_payoff}
\alias{confusion_payoff}
\title{Confusion matrix and payoff at a single threshold}
\usage{
confusion_payoff(
  x,
  threshold = 0.5,
  var_cost = 0,
  prob_accept = 1,
  tp_val = 0,
  fp_val = 0,
  tn_val = 0,
  fn_val = 0,
  prob_col = NA,
  truth_col = NA,
  positive = "Yes"
)
}
\arguments{
\item{x}{A data frame containing predicted probabilities of a target event and the actual outcome/class.}

\item{threshold}{Probability cutoff above which an observation is classified as the positive class.}

\item{var_cost}{Variable cost (e.g. of a campaign offer).}

\item{prob_accept}{Probability of offer being accepted. Defaults to 1.}

\item{tp_val}{The average value of a True Positive. `var_cost` is automatically subtracted.}

\item{fp_val}{The average cost of a False Positive. `var_cost` is automatically subtracted.}

\item{tn_val}{The average value of a True Negative.}

\item{fn_val}{The average cost of a False Negative.}

\item{prob_col}{The unquoted name of the column with probabilities of the event of interest.}

\item{truth_col}{The unquoted name of the column with the actual outcome/class.}

\item{positive}{The value in `truth_col` that identifies the event of interest. Defaults to 'Yes'.}
}
\value{
A one-row data frame with the following columns:

threshold = the threshold used \cr
tp        = number of true positives \cr
fp        = number of false positives \cr
tn        = number of true negatives \cr
fn        = number of false negatives \cr
payoff    = total payoff at the threshold
}
\description{
Classifies observations at a single probability threshold, returns the
resulting confusion matrix (TP/FP/TN/FN) and the associated payoff. This is
the single-threshold companion to [profit_thresholds()], which sweeps every
threshold, and uses the same value model.
}
\examples{
confusion_payoff(predictions,
   threshold   = 0.3,
   var_cost    = 100,
   prob_accept = .8,
   tp_val      = 2000,
   fp_val      = 0,
   tn_val      = 0,
   fn_val      = -2000,
   prob_col    = Yes,
   truth_col   = Churn)
}
