% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tornado.R
\name{tornado}
\alias{tornado}
\title{Tornado sensitivity analysis of maximum profit}
\usage{
tornado(
  x,
  fixed_cost = 0,
  var_cost = 0,
  tp_val = 0,
  prob_col = NA,
  truth_col = NA,
  positive = "Yes",
  variation = 0.2
)
}
\arguments{
\item{x}{A data frame containing predicted probabilities of a target event and the actual outcome/class.}

\item{fixed_cost}{Baseline fixed cost.}

\item{var_cost}{Baseline variable cost per targeted customer.}

\item{tp_val}{Baseline average value of a True Positive.}

\item{prob_col}{The unquoted name of the column with probabilities of the event of interest.}

\item{truth_col}{The unquoted name of the column with the actual outcome/class.}

\item{positive}{The value in `truth_col` that identifies the event of interest. Defaults to 'Yes'.}

\item{variation}{Fraction by which each parameter is moved up and down. Defaults to 0.2 (+/- 20 percent).}
}
\value{
A data frame, one row per parameter, ordered by descending `swing`:

parameter    = the assumption varied \cr
low_value    = parameter value on the low side \cr
high_value   = parameter value on the high side \cr
low_profit   = maximum profit at the low value \cr
high_profit  = maximum profit at the high value \cr
base_profit  = maximum profit at the baseline values \cr
swing        = absolute difference between low_profit and high_profit
}
\description{
Varies each cost/value assumption up and down by a fixed fraction, one at a
time, and records how the maximum achievable profit (the peak of the
[profit()] curve) responds. The result feeds a tornado plot, ordering the
assumptions by how much they move the bottom line.
}
\examples{
tornado(predictions,
   fixed_cost = 1000,
   var_cost   = 100,
   tp_val     = 2000,
   prob_col   = Yes,
   truth_col  = Churn)
}
