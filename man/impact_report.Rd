% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impact_report.R
\name{impact_report}
\alias{impact_report}
\title{Render a model-impact report}
\usage{
impact_report(
  x,
  prob_col,
  truth_col,
  positive = "Yes",
  fixed_cost = 0,
  var_cost = 0,
  tp_val = 0,
  prob_accept = 1,
  budget = NULL,
  output_file = "impact-report.html",
  quiet = TRUE
)
}
\arguments{
\item{x}{A data frame containing predicted probabilities of a target event
and the actual outcome/class.}

\item{prob_col}{The unquoted (or quoted) name of the column with the
probabilities of the event of interest.}

\item{truth_col}{The unquoted (or quoted) name of the column with the actual
outcome/class.}

\item{positive}{The value in `truth_col` that identifies the event of interest.}

\item{fixed_cost}{Fixed cost (e.g. of a campaign).}

\item{var_cost}{Variable cost per targeted case (a single value).}

\item{tp_val}{The value of a True Positive (a single value).}

\item{prob_accept}{Probability of the offer being accepted. Defaults to 1.}

\item{budget}{Optional budget. When supplied, a budget section is added to
the report.}

\item{output_file}{Path of the HTML file to create. Defaults to
`"impact-report.html"` in the working directory.}

\item{quiet}{Suppress Quarto's rendering output? Defaults to `TRUE`.}
}
\value{
The path to the rendered report, invisibly.
}
\description{
Renders a ready-made, parameterised \pkg{quarto} report bundled with the
package. The report pulls together the headline `impact_summary()` and the
cost/revenue, profit, ROI, gains, lift and marginal-profit views (plus a
budget section when a `budget` is supplied) into a single HTML document.
}
\details{
Requires the \pkg{quarto} R package and a working
[Quarto](https://quarto.org) installation.
}
\examples{
\dontrun{
impact_report(predictions,
   prob_col   = Yes,
   truth_col  = Churn,
   fixed_cost = 1000,
   var_cost   = 100,
   tp_val     = 2000,
   output_file = "churn-impact.html")
}
}
