% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/payoff_grid.R
\name{payoff_grid}
\alias{payoff_grid}
\title{Payoff sensitivity grid}
\usage{
payoff_grid(
  x,
  thresholds = seq(0, 1, 0.02),
  prob_accept = seq(0, 1, 0.1),
  var_cost = 0,
  tp_val = 0,
  fp_val = 0,
  tn_val = 0,
  fn_val = 0,
  prob_col = NA,
  truth_col = NA,
  positive = "Yes"
)
}
\arguments{
\item{x}{A data frame containing predicted probabilities of a target event and the actual outcome/class.}

\item{thresholds}{A vector of probability cutoffs to evaluate. Defaults to `seq(0, 1, 0.02)`.}

\item{prob_accept}{A vector of offer-acceptance probabilities to evaluate. Defaults to `seq(0, 1, 0.1)`.}

\item{var_cost}{Variable cost (e.g. of a campaign offer).}

\item{tp_val}{The average value of a True Positive. `var_cost` is automatically subtracted.}

\item{fp_val}{The average cost of a False Positive. `var_cost` is automatically subtracted.}

\item{tn_val}{The average value of a True Negative.}

\item{fn_val}{The average cost of a False Negative.}

\item{prob_col}{The unquoted name of the column with probabilities of the event of interest.}

\item{truth_col}{The unquoted name of the column with the actual outcome/class.}

\item{positive}{The value in `truth_col` that identifies the event of interest. Defaults to 'Yes'.}
}
\value{
A data frame with the following columns:

threshold   = classification threshold \cr
prob_accept = offer-acceptance probability \cr
payoff      = payoff for that combination
}
\description{
Computes payoff across a grid of classification thresholds and offer-acceptance
probabilities, so the robustness of the optimal operating point can be judged
at a glance (for example as a heatmap). Uses the same value model as
[profit_thresholds()] and [confusion_payoff()].
}
\examples{
payoff_grid(predictions,
   var_cost  = 100,
   tp_val    = 2000,
   fn_val    = -2000,
   prob_col  = Yes,
   truth_col = Churn)
}
